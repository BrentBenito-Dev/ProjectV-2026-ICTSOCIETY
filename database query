CREATE DATABASE alay;

USE alay;


CREATE TABLE MusicOrders (
    OrderID INT AUTO_INCREMENT PRIMARY KEY,
    Customer_Name VARCHAR(255),
    Song_Name VARCHAR(255),
    Target_Person VARCHAR(255),
    Order_Category VARCHAR(50),
    Status VARCHAR(20) DEFAULT 'Pending',
    Timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

use alay;

ALTER TABLE orders 
ADD COLUMN Rate_Type VARCHAR(50) DEFAULT 'Standard',
ADD COLUMN Price DECIMAL(10, 2) DEFAULT 0.00;

ps: dahil sa changes for report page
